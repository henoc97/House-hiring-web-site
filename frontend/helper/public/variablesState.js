


/**
 * @fileoverview This script defines variables and functions for managing prices and lists of tenants and properties.
 */

/**
 * Price for registering a home.
 * @constant {number}
 */
let registerHomePrice = 500;

/**
 * Price for registering a tenant.
 * @constant {number}
 */
let registerTenant = 300;

/**
 * Price for inserting a picture.
 * @constant {number}
 */
let insertPic = 500;

/**
 * Monthly price for a tenant.
 * @constant {number}
 */
let tenantMonthlyPrice = 200;

/**
 * Price for validating a receipt, calculated as 1% of the rent (minimum 200 XOF).
 * @constant {number}
 */
let validateReceiptPrice = 0.01; // 1% of the rent (min. 200 XOF)
